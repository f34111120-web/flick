# Flick — Supabase 連線設定
# 1. 把這個檔複製成 .env.local（不要含 .example）
# 2. 去 Supabase 專案 → Settings → Data API & API keys 拿值貼進來
# 3. 變數名一定要 VITE_ 開頭，否則 Vite build 後前端讀不到（會是 undefined）

VITE_SUPABASE_URL="https://你的專案.supabase.co"
VITE_SUPABASE_ANON_KEY="你的 publishable / anon key"
